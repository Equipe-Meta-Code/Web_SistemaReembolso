import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    pagina: {
        padding: 10,
        paddingHorizontal: 20,
    },
    section: {
        marginBottom: 24,
    },
    sectionTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 8,
    },
});